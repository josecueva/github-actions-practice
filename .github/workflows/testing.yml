name: hello-world
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v6
        - name: Print Hello World step
          run: |
            echo "Hello world"